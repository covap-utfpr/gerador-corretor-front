class TestHeader {
    
    constructor(title, institution, subject, date, instructions, value) {
        this.title = title;
        this.institution = institution;
        this.subject = subject;
        this.date = date;
        this.instructions = instructions;
        this.value = value;
    }
}

export default TestHeader;