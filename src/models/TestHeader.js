class TestHeader {
    
    constructor(title, institution, subject, date, instructions, value, parentId) {
        this.title = title;
        this.institution = institution;
        this.subject = subject;
        this.date = date;
        this.instructions = instructions;
        this.value = value;
        this.parentId = parentId;
    }
}

export default TestHeader;