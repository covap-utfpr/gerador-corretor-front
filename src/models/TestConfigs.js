
class TestConfigs {
    
    constructor(order, answCardPosition, font, fontSize, lines) {

        this.order = order;
        this.answCardPosition = answCardPosition;
        this.font = font;
        this.fontSize = fontSize;
        this.lines = lines;
    }

}

export default TestConfigs;
